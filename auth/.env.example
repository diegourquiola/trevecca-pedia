# Server Configuration
PORT=8083

# Database Configuration
# Set POSTGRES_PASSWORD in auth-db/.env to match the value used here
DATABASE_URL=postgres://auth_user:POSTGRES_PASSWORD@localhost:5433/auth?sslmode=disable

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXP_HOURS=24

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Development Configuration
# WARNING: Only enable in development! Creates a test user: dev@trevecca.edu / devpass
DEV_SEED=false
